<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.themesandstyles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThemesAndStyles">
        <activity android:name=".SplashScreen"></activity>
        <activity android:name=".advanced.CordinateLayout" />
        <activity android:name=".advanced.MainActivity2" />
        <activity android:name=".basics.keyFrameAttributes" />
        <activity android:name=".basics.SaturationActivity" />
        <activity android:name=".basics.CustomAttributes" />
        <activity android:name=".basics.ConstraintSetExample" />
        <activity android:name=".basics.PagerActivity" />
        <activity android:name=".basics.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>